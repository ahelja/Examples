{
    IBClasses = (
        {CLASS = EffectStackBox; LANGUAGE = ObjC; SUPERCLASS = NSBox; }, 
        {
            ACTIONS = {
                filterCancelButtonAction = id; 
                filterImageButtonAction = id; 
                filterOKButtonAction = id; 
                filterTextButtonAction = id; 
                minusButtonAction = id; 
                playButtonAction = id; 
                plusButtonAction = id; 
                presetPopUpButtonAction = id; 
                resetButtonAction = id; 
                tableViewDoubleClick = id; 
                topPlusButtonAction = id; 
            }; 
            CLASS = EffectStackController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                categoryTableView = NSTableView; 
                filterCancelButton = NSButton; 
                filterOKButton = NSButton; 
                filterPalette = NSWindow; 
                filterTableView = NSTableView; 
                filterTextButton = NSButton; 
                playButton = NSButton; 
                presetPopUpButton = NSPopUpButton; 
                resetButton = NSButton; 
                topPlusButton = NSButton; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}