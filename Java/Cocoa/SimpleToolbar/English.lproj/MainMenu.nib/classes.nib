{
    IBClasses = (
        {
            ACTIONS = {runToolbarCustomizationPalette = id; toggleToolbarShown = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}