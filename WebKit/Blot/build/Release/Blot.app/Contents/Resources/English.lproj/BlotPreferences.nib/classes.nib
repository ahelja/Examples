{
    IBClasses = (
        {
            ACTIONS = {checkSpellingClicked = id; defaultEditingFilePathChanged = id; }; 
            CLASS = BlotPreferences; 
            LANGUAGE = ObjC; 
            OUTLETS = {defaultEditingFilePath = NSTextField; inlineSpellCheckingSwitch = NSButton; }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}